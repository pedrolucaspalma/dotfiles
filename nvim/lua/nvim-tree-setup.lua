require("nvim-tree").setup({
  sort = {
    sorter = "case_sensitive",
  },
  view = {
    width = 30,
  },
  renderer = {
    group_empty = true,
  },
  filters = {
    dotfiles = true,
  },
})

--Open filetree
vim.keymap.set('n', '<C-b>', ':NvimTreeToggle<cr>')
